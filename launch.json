{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start Smart Financial Hub (Backend + Frontend)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://127.0.0.1:5500",
      "webRoot": "${workspaceFolder}/frontend",
      "preLaunchTask": "run-all"
    }
  ]
}
